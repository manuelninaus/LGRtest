title: "Living Game Repository"
description: "Psychologische Spielanalysen aus Lehre, Learning Lab und studentischer Wissensproduktion"
lang: de-AT

# If you want to use a GitHub Pages theme, you could set:
# theme: jekyll-theme-primer
# For now, you're using custom layouts, so `theme: null` is fine.
theme: null

collections:
  analyses:
    output: true
    permalink: /analyses/:name/

defaults:
  - scope:
      path: ""
      type: "analyses"
    values:
      layout: analysis

markdown: kramdown

kramdown:
  input: GFM

exclude:
  - README.md
